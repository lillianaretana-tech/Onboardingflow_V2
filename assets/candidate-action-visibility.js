(function(){'use strict';function clean(){document.querySelectorAll('#inbox .person').forEach(card=>{const status=(card.querySelector('.pill')?.textContent||'').toLowerCase();if(!status.includes('pendiente')&&!status.includes('devuelto'))card.querySelector('[data-approve-id]')?.remove()})}document.addEventListener('DOMContentLoaded',()=>{clean();new MutationObserver(clean).observe(document.getElementById('inbox'),{childList:true,subtree:true})})})();
